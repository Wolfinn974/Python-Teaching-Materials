# 🧩 01 – Basics

## 🎯 Objectif du module
Découvrir les bases de Python : affichage, variables, entrées utilisateur et conditions simples.  
Ces exercices posent les fondations de la logique de programmation avant d’aborder les boucles et les structures de données.

---

## 🧠 Rappels de notions vues

| Notion | Description courte | Exemple |
|--------|--------------------|----------|
| `print()` | Affiche un texte ou une valeur à l’écran. | `print("Hello world!")` |
| Variables | Permettent de stocker des données. | `name = "Alex"` |
| `input()` | Récupère une donnée saisie par l’utilisateur. | `age = input("Quel âge as-tu ? ")` |
| Conversions de types | Transformer une chaîne en nombre ou inversement. | `int("5")` → `5` |
| Conditions (`if`, `elif`, `else`) | Permettent de faire des choix selon une condition. | `if x > 0: print("Positif")` |

---

## 🧩 Exercices inclus

### 1. `print_alphabet.py` et `print_inversed_alphabet.py`
Afficher toutes les lettres de l’alphabet de A à Z sur une seule ligne.  
> 🧰 Notions : boucles, `chr()`, `range()`.

Afficher l’alphabet de Z à A.  
> 🧰 Notions : boucles inversées, `range()` avec pas négatif.

---

### 2. `is_it_positive.py`
Demander un nombre et afficher s’il est positif, négatif ou nul.  
> 🧰 Notions : `input()`, conversion en entier, conditions.

---

## 💬 Conseils

- Expérimente avec d’autres alphabets ou caractères pour t’amuser avec `chr()` et `ord()`.  
- Ajoute des vérifications : et si l’utilisateur entre du texte au lieu d’un nombre ?  
- Essaie de commenter ton code à chaque étape pour expliquer ta logique.  

---

## 🧾 Auteur
Ces exercices ont été créés pour mes élèves en initiation à Python.  
> Libre d’utilisation et de modification à des fins pédagogiques.  